---
phase: 06-chat-ui-rewrite
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/assistant-ui/save-conversation-button.tsx
  - components/assistant-ui/saved-thread-loader.tsx
  - app/c/[id]/page.tsx
  - app/page.tsx
  - components/brief/brief-page-content.tsx
autonomous: true
requirements: [UX-03]

must_haves:
  truths:
    - "Save button shows 'Saved' after successful save and disables further clicks"
    - "Save calls Dify conversation rename API to auto-generate title"
    - "URL changes to /c/{id} after save without full page reload"
    - "Toast notification appears after successful save"
    - "Once saved, new messages auto-persist via threadId in chat request"
    - "New conversation button resets to fresh state"
    - "Saved conversation at /c/{id} loads messages and sets conversation_id for continuation"
    - "Loading spinner shown while saved messages are being imported"
  artifacts:
    - path: "components/assistant-ui/save-conversation-button.tsx"
      provides: "Save button with Saved state, toast, Dify rename, auto-persist, New conversation"
      contains: "auto_generate"
    - path: "components/assistant-ui/saved-thread-loader.tsx"
      provides: "Message import with loading state"
      contains: "setLoading"
    - path: "app/c/[id]/page.tsx"
      provides: "Saved conversation resume page"
      contains: "SavedThreadLoader"
  key_links:
    - from: "save-conversation-button.tsx"
      to: "/api/brief/conversations/[id]/name"
      via: "fetch POST for auto-generated title"
      pattern: "conversations.*name"
    - from: "save-conversation-button.tsx"
      to: "brief-runtime-provider.tsx"
      via: "useSavedThreadId and useConversationId contexts"
      pattern: "useSavedThreadId|useConversationId"
    - from: "saved-thread-loader.tsx"
      to: "/api/threads/[id]/messages"
      via: "fetch GET for saved messages"
      pattern: "api/threads.*messages"
---

<objective>
Complete the save-on-demand conversation persistence flow: Dify auto-generated titles, toast confirmation, button state management, auto-persist after initial save, New conversation reset, and saved conversation resume improvements.

Purpose: Users need to explicitly save conversations worth keeping. Once saved, the experience should be seamless -- auto-persist new messages, show saved state, and resume correctly from /c/{id}.

Output: Working save flow with Dify-generated titles, toast, URL update, auto-persist, and improved resume page.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-chat-ui-rewrite/06-CONTEXT.md
@.planning/phases/06-chat-ui-rewrite/06-RESEARCH.md

<interfaces>
<!-- Contexts from brief-runtime-provider.tsx (created by Plan 01) -->
```typescript
// From components/assistant-ui/brief-runtime-provider.tsx
export const ConversationIdContext: React.Context<React.MutableRefObject<string>>;
export function useConversationId(): React.MutableRefObject<string>;
export const SavedThreadIdContext: React.Context<React.MutableRefObject<string>>;
export function useSavedThreadId(): React.MutableRefObject<string>;

// BriefRuntimeProvider wraps children with both contexts
```

From app/api/threads/route.ts:
```typescript
POST /api/threads -> { id: string }  // creates thread with active agent snapshot
```

From app/api/threads/[id]/route.ts:
```typescript
GET /api/threads/{id} -> { id, title, dify_conversation_id, ... }
PATCH /api/threads/{id} <- { title?, difyConversationId? }
DELETE /api/threads/{id}
```

From app/api/threads/[id]/messages/route.ts:
```typescript
GET /api/threads/{id}/messages -> { messages: { role, content }[] }
POST /api/threads/{id}/messages <- { messages: { role, content }[] }
```

From app/api/brief/conversations/[id]/name/route.ts (created by Plan 01):
```typescript
POST /api/brief/conversations/{dify_conversation_id}/name <- { auto_generate: true }
-> { id, name, ... }
```

From app/api/brief/chat/route.ts:
```typescript
POST /api/brief/chat <- { message, conversation_id, threadId?, files? }
// When threadId is provided, auto-persists messages to DB
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite save button with Dify rename, toast, auto-persist, and New conversation</name>
  <files>
    components/assistant-ui/save-conversation-button.tsx
  </files>
  <read_first>
    components/assistant-ui/save-conversation-button.tsx,
    components/assistant-ui/brief-runtime-provider.tsx,
    .planning/phases/06-chat-ui-rewrite/06-RESEARCH.md
  </read_first>
  <action>
Rewrite `save-conversation-button.tsx` to implement D-19 through D-23:

**Save button component:**
- Import `useConversationId` and `useSavedThreadId` from `./brief-runtime-provider`.
- Import `useAssistantRuntime` from `@assistant-ui/react`.
- Track state: `status: 'idle' | 'saving' | 'saved'`.
- When on `/c/{id}` page (detect via `useSavedThreadId().current !== ''`), initialize status to `'saved'`.

**handleSave logic (per RESEARCH.md save flow):**
1. Get messages from `runtime.thread.getState().messages`.
2. If no messages, return early.
3. Set status to `'saving'`.
4. POST to `/api/threads` to create thread -- get `threadId`.
5. Serialize messages: for each message, extract text from content parts, use role.
6. POST to `/api/threads/${threadId}/messages` with serialized messages.
7. If `conversationIdRef.current` is not empty, POST to `/api/brief/conversations/${conversationIdRef.current}/name` with `{ auto_generate: true }` to get Dify-generated title. Fall back to first user message text (sliced to 50 chars) if API fails.
8. PATCH `/api/threads/${threadId}` with `{ title, difyConversationId: conversationIdRef.current }`.
9. Update `savedThreadIdRef.current = threadId` -- this enables auto-persist in the chat API route (Plan 01's adapter passes threadId in request body when savedThreadIdRef.current is set).
10. `window.history.replaceState(null, '', basePath + '/c/' + threadId)` -- URL changes without reload.
11. Show toast: use a simple inline toast div positioned at bottom-center with "Conversation saved" text, auto-dismiss after 3 seconds. Use `useState` for toast visibility.
12. Set status to `'saved'`.

**Button rendering:**
- Disabled when: status is `'saving'` OR `runtime.thread.getState().messages.length === 0` (use `useAuiState` for reactive empty check).
- Text: `'Save'` when idle, `'Saving...'` when saving, `'Saved'` (with CheckIcon) when saved.
- `Save` icon (lucide) for idle, `Check` icon for saved state.

**New conversation button:**
- Add a separate "New" button (PlusIcon) next to Save button.
- On click: set `savedThreadIdRef.current = ''`, `conversationIdRef.current = ''`, set status to `'idle'`.
- Navigate to `basePath + '/'` using `window.location.href` (full reload to reset runtime).

**Toast implementation (Claude's discretion):**
- Use a simple absolute-positioned div at the bottom of the screen with `animate-in fade-in slide-in-from-bottom` classes.
- Auto-dismiss after 3s via setTimeout.
- Green checkmark icon + "Conversation saved" text.

**Auto-persist after save (D-22):**
- After save, the chat adapter in `brief-runtime-provider.tsx` needs to pass `threadId` in the fetch body to `/api/brief/chat`. Update the adapter factory to accept `savedThreadIdRef` and include `threadId: savedThreadIdRef.current || undefined` in the POST body.
- This requires updating `createDifyChatAdapter` signature in Plan 01's file. Since Plan 01 already adds `SavedThreadIdContext`, the adapter should read from `savedThreadIdRef` -- add this parameter to `createDifyChatAdapter` and pass it from the provider.

Note: This task must also update `brief-runtime-provider.tsx` to pass `savedThreadIdRef` to the adapter. Add `savedThreadIdRef` as the third parameter to `createDifyChatAdapter`, and in the fetch body add: `threadId: savedThreadIdRef.current || undefined`.
  </action>
  <verify>
    <automated>cd /Users/charli.idrac/dev/work/email_generation/Conversational-Email-Editor && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - save-conversation-button.tsx contains `auto_generate: true` in a fetch to conversations rename endpoint
    - save-conversation-button.tsx contains `window.history.replaceState` for URL update without reload
    - save-conversation-button.tsx contains `status === 'saved'` conditional rendering with CheckIcon
    - save-conversation-button.tsx contains a toast div with "Conversation saved" text
    - save-conversation-button.tsx contains `savedThreadIdRef.current = threadId` for auto-persist linking
    - save-conversation-button.tsx contains a New conversation button with PlusIcon that resets state
    - brief-runtime-provider.tsx contains `threadId: savedThreadIdRef.current` in the chat fetch body
  </acceptance_criteria>
  <done>Save button calls Dify rename API, shows toast, updates URL, tracks saved state, enables auto-persist, and New button resets fresh state</done>
</task>

<task type="auto">
  <name>Task 2: Improve saved conversation resume page with loading state and proper initialization</name>
  <files>
    components/assistant-ui/saved-thread-loader.tsx,
    app/c/[id]/page.tsx
  </files>
  <read_first>
    components/assistant-ui/saved-thread-loader.tsx,
    app/c/[id]/page.tsx,
    components/assistant-ui/brief-runtime-provider.tsx,
    .planning/phases/06-chat-ui-rewrite/06-RESEARCH.md
  </read_first>
  <action>
**A) Update SavedThreadLoader** in `saved-thread-loader.tsx`:
- Add `loading` state (boolean) and expose it via a callback prop `onLoadingChange?: (loading: boolean) => void`.
- Set loading to true at start, false after import completes or on error.
- Add error handling: if fetch fails, call `onError?: (error: string) => void` callback.
- Per D-28: if messages load but Dify API might fail on resume, still show messages (read-only fallback handled by adapter's error throw).

**B) Update ConversationPage** in `app/c/[id]/page.tsx`:
- Import `useSavedThreadId` from `brief-runtime-provider`.
- On mount, after fetching thread data, set `savedThreadIdRef.current = id` so auto-persist works for new messages.
- Also set `conversationIdRef` from thread's `dify_conversation_id` (already partially done via `initialConversationId` prop).
- Add loading state: show a centered spinner (simple div with `animate-spin` border) while `SavedThreadLoader` is importing messages.
- Per D-27: loading spinner overlay on the chat panel during import.
- The `SaveConversationButton` should auto-detect saved state since `savedThreadIdRef.current` will be set on mount.
- Add a `useEffect` that sets `savedThreadIdRef.current = id` when the page mounts. Access the ref via `useSavedThreadId()` hook (from SavedThreadIdContext).

**C) Loading spinner design (Claude's discretion):**
- Simple centered spinner: `<div className="flex items-center justify-center h-full"><div className="h-8 w-8 animate-spin rounded-full border-2 border-muted-foreground border-t-transparent" /></div>`.
- Show instead of `BriefThread` while loading is true.
  </action>
  <verify>
    <automated>cd /Users/charli.idrac/dev/work/email_generation/Conversational-Email-Editor && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - saved-thread-loader.tsx contains `onLoadingChange` prop
    - saved-thread-loader.tsx sets loading to false after ExportedMessageRepository.fromArray and import complete
    - app/c/[id]/page.tsx contains `useSavedThreadId` import and `savedThreadIdRef.current = id`
    - app/c/[id]/page.tsx contains a loading spinner div with `animate-spin`
    - app/c/[id]/page.tsx conditionally renders spinner vs BriefThread based on loading state
  </acceptance_criteria>
  <done>Resume page initializes savedThreadId for auto-persist, shows loading spinner during message import, correctly resumes Dify conversation via stored conversation_id</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Save button shows three states: Save, Saving..., Saved
- New conversation button exists with PlusIcon
- Toast notification div present in save button component
- Resume page shows loading spinner during import
- savedThreadIdRef is set on /c/{id} pages for auto-persist
</verification>

<success_criteria>
- Save flow: creates thread, saves messages, calls Dify rename, updates URL, shows toast, transitions to Saved state
- Auto-persist: after initial save, threadId flows to chat API for automatic message persistence
- New conversation: resets all state and navigates to /
- Resume: /c/{id} loads messages with spinner, sets conversation_id for continuation, shows Saved state
</success_criteria>

<output>
After completion, create `.planning/phases/06-chat-ui-rewrite/06-02-SUMMARY.md`
</output>

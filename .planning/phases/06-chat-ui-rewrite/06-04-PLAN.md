---
phase: 06-chat-ui-rewrite
plan: 04
type: execute
wave: 3
depends_on: [06-01, 06-02, 06-03]
files_modified: []
autonomous: false
requirements: [UX-01, UX-02, UX-03, UX-04, UX-06, UX-07]

must_haves:
  truths:
    - "Opener appears on empty thread with opening statement from Dify /parameters"
    - "Suggested questions send real messages when clicked"
    - "3-dot thinking indicator appears while assistant is working"
    - "Reasoning section appears after response with agent_thought data"
    - "Like/dislike buttons submit feedback and show filled state"
    - "Save creates thread, gets Dify-generated title, shows toast, updates URL"
    - "Saved conversation resumes at /c/{id} with messages loaded"
    - "Copy and Regenerate work on assistant messages"
  artifacts: []
  key_links: []
---

<objective>
End-to-end verification of all Phase 6 features. This is a human verification checkpoint covering D-07 (opener/suggestions testing), D-12 (feedback testing), D-16 (reasoning testing), and D-24 (save flow testing).

Purpose: Confirm all features work together in the real application before marking Phase 6 complete.

Output: Human-verified working chat UI with all features functional.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-chat-ui-rewrite/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and smoke-test the application</name>
  <files></files>
  <read_first>
    components/assistant-ui/brief-thread.tsx,
    components/assistant-ui/brief-runtime-provider.tsx,
    components/assistant-ui/save-conversation-button.tsx
  </read_first>
  <action>
1. Run `npm run build` to verify the application compiles without errors.
2. If build fails, fix any type errors or import issues in the files modified by Plans 01-03.
3. Run `npm run dev` in background to start the dev server.
4. Verify server starts without errors on stdout.

Common issues to check and fix:
- Missing imports (SuggestionPrimitive, ThumbsUp, ThumbsDown, ChevronRightIcon, Badge)
- Type mismatches in ChatModelRunResult (ensure reasoning parts are correctly typed)
- useAui import source (@assistant-ui/react vs @assistant-ui/store)
- Feedback adapter type compatibility with useLocalRuntime options
  </action>
  <verify>
    <automated>cd /Users/charli.idrac/dev/work/email_generation/Conversational-Email-Editor && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0
    - No TypeScript errors in build output
    - No missing module errors
  </acceptance_criteria>
  <done>Application compiles and starts successfully</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all Phase 6 features</name>
  <files></files>
  <action>Present the verification steps to the user and wait for approval. The dev server should already be running from Task 1.</action>
  <what-built>Complete chat UI rewrite: opener with suggestions, feedback buttons, reasoning display, save flow, conversation resume, attachment handling</what-built>
  <how-to-verify>
**Start the app:** `npm run dev` then visit http://localhost:3000

**Test 1: Opener and Suggestions (D-07)**
1. Open http://localhost:3000 -- should see opening statement from Dify /parameters
2. Below opener, see suggested questions as clickable buttons
3. Click a suggestion -- should send as user message and get assistant response
4. Opener and suggestions should disappear once message is sent

**Test 2: Thinking Indicator and Message Flow (D-09, D-13)**
1. Type a message and send
2. While assistant is responding, see 3 bouncing dots
3. Dots replaced by actual text as response streams in
4. Text renders as markdown (bold, lists, links)

**Test 3: Reasoning Display (D-16)**
(Only if the configured Dify agent uses agent mode with tools)
1. Send a message that triggers tool use
2. After response completes, look for "Show reasoning" toggle below the message
3. Click toggle -- reasoning text expands with tool badges as pills
4. Click again -- collapses

**Test 4: Action Bar and Feedback (D-12)**
1. Hover over an assistant message -- see Copy and Regenerate buttons
2. Click Copy -- icon changes to checkmark briefly
3. Like/dislike buttons should be always visible (not just on hover)
4. Click Like on a message -- thumbs up fills
5. Click Dislike on another -- thumbs down fills

**Test 5: Save Flow (D-24)**
1. After a conversation, click "Save" button in header
2. Should see "Saving..." then "Saved" with check icon
3. Toast notification appears at bottom saying "Conversation saved"
4. URL changes to /c/{some-id} without page reload
5. Send another message -- it should work (auto-persist)

**Test 6: Conversation Resume**
1. Click "New" button -- navigates to / with fresh state
2. Open history drawer (History icon) -- see the saved conversation
3. Click it -- navigates to /c/{id}
4. Messages load with spinner then display
5. Send a new message -- conversation continues

**Test 7: File Upload (D-17)**
1. Click paperclip in composer -- file dialog opens
2. Select an image -- preview chip appears in composer
3. Can remove with X button
4. Drag a file onto the composer -- drop zone highlights
  </how-to-verify>
  <verify>Human confirms all 7 test areas pass</verify>
  <acceptance_criteria>
    - All 7 test areas pass without JavaScript console errors
    - User types "approved" to proceed
  </acceptance_criteria>
  <done>All Phase 6 features verified working by human tester</done>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All 7 test areas pass as described in the checkpoint task.
</verification>

<success_criteria>
- All features from Phase 6 CONTEXT.md decisions verified working
- Opener, suggestions, feedback, reasoning, save flow, resume, and attachments all functional
- No JavaScript console errors during normal usage
</success_criteria>

<output>
After completion, create `.planning/phases/06-chat-ui-rewrite/06-04-SUMMARY.md`
</output>

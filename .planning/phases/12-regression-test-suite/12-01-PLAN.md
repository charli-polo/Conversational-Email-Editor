---
phase: 12-regression-test-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/settings/page.tsx
  - components/settings/agent-list.tsx
  - components/settings/agent-form.tsx
  - components/settings/test-prompt-list.tsx
  - components/settings/test-prompt-form.tsx
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "/settings page loads without error in the browser"
    - "/settings agents tab renders the agent list"
    - "Agent CRUD operations (create, read, edit, delete) work end-to-end on /settings"
  artifacts:
    - path: "app/settings/page.tsx"
      provides: "Settings page that renders without runtime errors"
  key_links:
    - from: "app/settings/page.tsx"
      to: "/api/agents"
      via: "AgentList fetch"
      pattern: "fetch.*api/agents"
---

<objective>
Diagnose and fix the /settings page bug so the page loads without error.

Purpose: The /settings page currently has a bug preventing it from loading. This must be fixed before regression tests can cover it.
Output: Working /settings page, root cause documented in summary.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-regression-test-suite/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose and fix the /settings page bug</name>
  <files>app/settings/page.tsx, components/settings/agent-list.tsx, components/settings/agent-form.tsx, components/settings/test-prompt-list.tsx, components/settings/test-prompt-form.tsx</files>
  <read_first>
    - app/settings/page.tsx
    - components/settings/agent-list.tsx
    - components/settings/agent-form.tsx
    - components/settings/test-prompt-list.tsx
    - components/settings/test-prompt-form.tsx
    - components/settings/delete-confirm-dialog.tsx
    - components/settings/settings-sheet.tsx
    - app/api/agents/route.ts
    - app/api/agents/[id]/route.ts
    - app/api/test-prompts/route.ts
    - app/api/test-prompts/[id]/route.ts
    - lib/db/schema.ts
  </read_first>
  <action>
    Diagnose the /settings page bug using this procedure:

    1. Start the dev server on port 3001 with test DB: `DATABASE_PATH=./data/test-db.sqlite npx next dev --port 3001`
    2. Navigate to http://localhost:3001/settings in a Playwright script or curl the page
    3. Check the Next.js server console and browser console for errors
    4. Common suspects for settings page bugs in this codebase:
       - Missing or stale Radix UI Tabs import (the page uses `Tabs`, `TabsList`, `TabsTrigger`, `TabsContent` from @/components/ui/tabs)
       - Database schema mismatch if test DB lacks agents/test_prompts tables
       - Client component hydration error from server-side DB call
       - Import path issues after v1.1 refactors
    5. Once root cause is identified, fix it in the relevant file(s)
    6. Verify fix by loading /settings in browser (via Playwright or dev server)

    Document the root cause in a comment at the top of the commit message.

    Per D-08: This is a diagnosis task — capture the error, identify root cause, fix it.
  </action>
  <verify>
    <automated>cd /Users/charli.idrac/dev/work/email_generation/Conversational-Email-Editor && npx playwright test --grep "settings" 2>/dev/null || echo "No settings test yet — verify manually: start dev server, navigate to /settings, confirm page renders without console errors"</automated>
  </verify>
  <acceptance_criteria>
    - /settings page loads in browser without JavaScript errors or blank screen
    - The "Settings" h1 heading is visible
    - The "Agents" and "Test Prompts" tabs are visible and clickable
    - The "Add agent" button is visible under the Agents tab
    - No hydration errors in browser console
  </acceptance_criteria>
  <done>/settings page loads without error, agents tab shows agent list, bug root cause documented</done>
</task>

<task type="auto">
  <name>Task 2: Verify /editor page loads without error</name>
  <files></files>
  <read_first>
    - app/editor/page.tsx
    - components/chat/chat-panel-with-assistant-ui.tsx
    - components/preview/email-preview.tsx
  </read_first>
  <action>
    Verify that /editor loads without error by navigating to it in a Playwright script:

    1. Write a quick inline Playwright test (or use the dev server) to confirm /editor renders
    2. The editor page imports ChatPanel, PropertyPanel, DesignEmptyState, EmailPreview, and uses SAMPLE_EMAIL
    3. It also references `process.env.NEXT_PUBLIC_APP_VERSION` and image assets at `/icons/aura-icon.svg` and `/icons/design-icon.svg`
    4. Common issues: missing image assets, undefined env var, client component import errors
    5. If /editor has issues, fix them following the same diagnosis approach as Task 1
    6. If /editor loads fine, no code changes needed — just confirm in verification

    Per D-04 flow 7: /editor page loads without error.
  </action>
  <verify>
    <automated>cd /Users/charli.idrac/dev/work/email_generation/Conversational-Email-Editor && DATABASE_PATH=./data/test-db.sqlite node -e "
const { chromium } = require('playwright');
(async () => {
  const browser = await chromium.launch();
  const page = await browser.newPage();
  await page.goto('http://localhost:3001/editor');
  await page.waitForLoadState('networkidle');
  const errors = [];
  page.on('pageerror', e => errors.push(e.message));
  const title = await page.textContent('h1');
  console.log('Page title:', title);
  console.log('Errors:', errors.length ? errors : 'none');
  await browser.close();
  process.exit(errors.length > 0 ? 1 : 0);
})();
" 2>&1 || echo "Manual check: navigate to http://localhost:3001/editor and confirm it renders"</automated>
  </verify>
  <acceptance_criteria>
    - /editor page loads without JavaScript console errors
    - The page shows the email preview area (EmailPreview component renders)
    - The mode toggle ("AI mode" / "Design mode") buttons are visible
    - No blank screen or hydration errors
  </acceptance_criteria>
  <done>/editor page confirmed working or fixed, ready for regression test coverage</done>
</task>

</tasks>

<verification>
- /settings page loads at http://localhost:3001/settings without errors
- /editor page loads at http://localhost:3001/editor without errors
- Both pages render their primary UI elements (headings, buttons, panels)
</verification>

<success_criteria>
- /settings bug identified, root cause documented, fix applied
- /editor confirmed working (or fixed if broken)
- Both routes ready for E2E regression test coverage in Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/12-regression-test-suite/12-01-SUMMARY.md`
</output>

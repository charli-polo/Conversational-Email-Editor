---
phase: 13-suggested-answer-parsing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/dify/parse-suggested-answer.ts
  - __tests__/parse-suggested-answer.test.ts
  - components/assistant-ui/brief-runtime-provider.tsx
autonomous: true
requirements:
  - PARSE-01
  - PARSE-02
  - PARSE-03

must_haves:
  truths:
    - "A pure function extracts the JSON actions array from a <suggested_answer> block in message text"
    - "The returned display text has the <suggested_answer> block completely removed"
    - "Messages without a <suggested_answer> block pass through unchanged with empty actions"
    - "Malformed JSON inside a <suggested_answer> block results in empty actions, not a crash"
    - "Partial <suggested_answer> tags during streaming do not leak into displayed text"
  artifacts:
    - path: "lib/dify/parse-suggested-answer.ts"
      provides: "Pure parser function and types"
      exports: ["parseSuggestedAnswer", "SuggestedAction", "ParseResult"]
    - path: "__tests__/parse-suggested-answer.test.ts"
      provides: "Unit tests for parser"
      min_lines: 60
    - path: "components/assistant-ui/brief-runtime-provider.tsx"
      provides: "Adapter integration calling parser during streaming"
      contains: "parseSuggestedAnswer"
  key_links:
    - from: "components/assistant-ui/brief-runtime-provider.tsx"
      to: "lib/dify/parse-suggested-answer.ts"
      via: "import { parseSuggestedAnswer }"
      pattern: "import.*parseSuggestedAnswer.*parse-suggested-answer"
    - from: "components/assistant-ui/brief-runtime-provider.tsx"
      to: "metadata.custom.suggestedActions"
      via: "final yield metadata"
      pattern: "suggestedActions"
---

<objective>
Create a pure parsing function that extracts `<suggested_answer>` JSON blocks from Dify agent message text, then integrate it into the streaming adapter so assistant messages are displayed without the raw block and actions data is passed via metadata.

Purpose: Enable Phase 14 to render clickable action chips by providing structured actions data on assistant messages.
Output: `lib/dify/parse-suggested-answer.ts` (parser), `__tests__/parse-suggested-answer.test.ts` (tests), updated `brief-runtime-provider.tsx` (integration).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-suggested-answer-parsing/13-RESEARCH.md

<interfaces>
<!-- Existing adapter pattern in brief-runtime-provider.tsx that parser must integrate with -->

From components/assistant-ui/brief-runtime-provider.tsx:
```typescript
// The adapter accumulates rawText from SSE chunks, extracts <Brief> blocks,
// and yields { content: [...], metadata?: { custom: { difyMessageId, toolBadges } } }
// Parser must be applied AFTER Brief extraction on displayText, BEFORE yield.
// On final yield, actions go into metadata.custom.suggestedActions.
```

From vitest.config.ts:
```typescript
// Tests go in __tests__/**/*.test.ts
// Uses globals: true (describe/it/expect available without import)
// Alias: '@' -> project root
```

From __tests__/dify-adapters.test.ts (test pattern):
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
// Tests use standard vitest patterns with vi.mock for module mocking
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create parseSuggestedAnswer function with TDD</name>
  <files>lib/dify/parse-suggested-answer.ts, __tests__/parse-suggested-answer.test.ts</files>
  <read_first>
    - components/assistant-ui/brief-runtime-provider.tsx (lines 124-168 for existing Brief extraction pattern to mirror)
    - __tests__/dify-adapters.test.ts (first 20 lines for vitest import/mock patterns)
    - vitest.config.ts (test config and alias setup)
    - .planning/phases/13-suggested-answer-parsing/13-RESEARCH.md (Pattern 1 and test cases)
  </read_first>
  <behavior>
    - Test: basic extraction -- input `"Text\n<suggested_answer>[{\"label\":\"A\",\"prompt\":\"B\"}]</suggested_answer>"` returns `{ displayText: "Text", actions: [{ label: "A", prompt: "B" }] }`
    - Test: no block -- input `"Just plain text"` returns `{ displayText: "Just plain text", actions: [] }`
    - Test: malformed JSON -- input `"Text\n<suggested_answer>not json</suggested_answer>"` returns `{ displayText: "Text", actions: [] }`
    - Test: empty actions array -- input `"Text\n<suggested_answer>[]</suggested_answer>"` returns `{ displayText: "Text", actions: [] }`
    - Test: block at start -- input `"<suggested_answer>[{\"label\":\"A\",\"prompt\":\"B\"}]</suggested_answer>\nMore text"` returns `{ displayText: "More text", actions: [{ label: "A", prompt: "B" }] }`
    - Test: block with extra whitespace -- input with newlines inside tag returns trimmed displayText and parsed actions
    - Test: invalid action objects filtered -- input with objects missing `label` or `prompt` are excluded from actions array
    - Test: empty string -- input `""` returns `{ displayText: "", actions: [] }`
    - Test: multiple actions -- input with 3 valid action objects returns all 3
  </behavior>
  <action>
    RED phase: Create `__tests__/parse-suggested-answer.test.ts` with all 9 test cases above. Import from `@/lib/dify/parse-suggested-answer`. Use vitest `describe`/`it`/`expect`. Run tests -- they must fail (module not found).

    GREEN phase: Create `lib/dify/parse-suggested-answer.ts` with:

    1. Export `interface SuggestedAction { label: string; prompt: string; }`
    2. Export `interface ParseResult { displayText: string; actions: SuggestedAction[]; }`
    3. Export `function parseSuggestedAnswer(text: string): ParseResult` that:
       - Uses regex `/<suggested_answer>([\s\S]*?)<\/suggested_answer>/` to find the block
       - If no match: return `{ displayText: text, actions: [] }`
       - If match: remove the block from text via `.replace(/<suggested_answer>[\s\S]*?<\/suggested_answer>/, '').trim()`
       - Try `JSON.parse(match[1].trim())` on extracted content
       - If parse fails: return `{ displayText, actions: [] }`
       - If parse succeeds: filter array to keep only objects where both `label` (string) and `prompt` (string) exist
       - Return `{ displayText, actions }`

    Run tests -- all must pass.

    REFACTOR: No refactoring expected for this simple function.

    Per PARSE-01: extracts JSON block. Per PARSE-02: strips block from display. Per PARSE-03: handles missing block gracefully.
  </action>
  <verify>
    <automated>cd /Users/charli.idrac/dev/work/email_generation/Conversational-Email-Editor && npx vitest run __tests__/parse-suggested-answer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/dify/parse-suggested-answer.ts` contains `export function parseSuggestedAnswer(`
    - `lib/dify/parse-suggested-answer.ts` contains `export interface SuggestedAction`
    - `lib/dify/parse-suggested-answer.ts` contains `export interface ParseResult`
    - `lib/dify/parse-suggested-answer.ts` contains `/<suggested_answer>([\s\S]*?)<\/suggested_answer>/`
    - `__tests__/parse-suggested-answer.test.ts` contains at least 9 `it(` test cases
    - `npx vitest run __tests__/parse-suggested-answer.test.ts` exits 0 with all tests passing
  </acceptance_criteria>
  <done>All 9 unit tests pass. Parser correctly extracts, strips, and handles edge cases for suggested_answer blocks.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate parser into streaming adapter</name>
  <files>components/assistant-ui/brief-runtime-provider.tsx</files>
  <read_first>
    - components/assistant-ui/brief-runtime-provider.tsx (full file -- understand current streaming logic)
    - lib/dify/parse-suggested-answer.ts (parser just created in Task 1 -- get exact exports)
  </read_first>
  <action>
    Modify `components/assistant-ui/brief-runtime-provider.tsx` to integrate `parseSuggestedAnswer`:

    1. Add import at top: `import { parseSuggestedAnswer } from '@/lib/dify/parse-suggested-answer';`

    2. In the `agent_thought` handler (around line 124-141):
       - After the existing Brief extraction that produces `displayText`, add:
         `const { displayText: saCleanText } = parseSuggestedAnswer(displayText);`
       - Change the yield to use `saCleanText` instead of `displayText` in the text content part
       - Also strip any trailing partial `<suggested_answer` open tag from `saCleanText` for streaming safety:
         ```
         let safeDisplay = saCleanText;
         const partialMatch = safeDisplay.match(/<suggested_answer[^>]*$/);
         if (partialMatch) {
           safeDisplay = safeDisplay.substring(0, partialMatch.index).trim();
         }
         ```

    3. In the `agent_message`/`message` handler (around line 144-169):
       - After Brief extraction produces `displayText` (line 158), add:
         `const { displayText: saCleanText } = parseSuggestedAnswer(displayText);`
       - Strip trailing partial tag same as above
       - Change the yield to use the safe display text instead of `displayText`

    4. In the final yield section (around line 191-213):
       - After Brief extraction produces `finalDisplayText` (line 199), add:
         `const { displayText: finalSaClean, actions: suggestedActions } = parseSuggestedAnswer(finalDisplayText);`
       - Change the final yield to use `finalSaClean` for text content
       - Add `suggestedActions` to `metadata.custom`:
         ```
         metadata: {
           custom: {
             difyMessageId,
             toolBadges,
             suggestedActions,
           },
         },
         ```

    This ensures: during streaming, `<suggested_answer>` content never shows in chat. On completion, parsed actions are available in message metadata for Phase 14 to consume.
  </action>
  <verify>
    <automated>cd /Users/charli.idrac/dev/work/email_generation/Conversational-Email-Editor && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `brief-runtime-provider.tsx` contains `import { parseSuggestedAnswer } from '@/lib/dify/parse-suggested-answer'`
    - `brief-runtime-provider.tsx` contains `parseSuggestedAnswer(displayText)` (called in agent_thought handler)
    - `brief-runtime-provider.tsx` contains `parseSuggestedAnswer(displayText)` (called in agent_message handler)
    - `brief-runtime-provider.tsx` contains `parseSuggestedAnswer(finalDisplayText)` (called in final yield section)
    - `brief-runtime-provider.tsx` contains `suggestedActions` in the metadata.custom object of the final yield
    - `brief-runtime-provider.tsx` contains `/<suggested_answer[^>]*$/` (partial tag stripping for streaming safety)
    - `npx tsc --noEmit` exits 0 (no type errors)
  </acceptance_criteria>
  <done>Adapter streams clean display text with suggested_answer blocks stripped, and the final yield includes parsed actions in metadata.custom.suggestedActions. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
1. `npx vitest run __tests__/parse-suggested-answer.test.ts` -- all 9+ unit tests pass
2. `npx tsc --noEmit` -- no type errors across the project
3. `grep -c "parseSuggestedAnswer" components/assistant-ui/brief-runtime-provider.tsx` returns 4 (1 import + 3 calls)
4. `grep "suggestedActions" components/assistant-ui/brief-runtime-provider.tsx` shows it in metadata.custom
</verification>

<success_criteria>
- Pure parser function exists at lib/dify/parse-suggested-answer.ts with exported types and function
- 9+ unit tests pass covering extraction, stripping, edge cases, and graceful degradation
- Adapter integration strips suggested_answer blocks during streaming (no leakage to UI)
- Final yield includes suggestedActions in metadata.custom for Phase 14 consumption
- TypeScript compiles clean with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/13-suggested-answer-parsing/13-01-SUMMARY.md`
</output>
